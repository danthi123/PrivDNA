services:
  privdna-web:
    build:
      context: ./site
      dockerfile: Dockerfile
      args:
        - RYBBIT_SITE_ID=${RYBBIT_SITE_ID}
        - RYBBIT_URL=${RYBBIT_URL}
    container_name: privdna-web
    restart: unless-stopped
    environment:
      - DATABASE_KEY=${DATABASE_KEY}
      - RYBBIT_SITE_ID=${RYBBIT_SITE_ID}
      - RYBBIT_URL=${RYBBIT_URL}
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - SMTP_FROM=${SMTP_FROM}
      - SMTP_SECURE=${SMTP_SECURE}
      - SMTP_REJECT_UNAUTHORIZED=${SMTP_REJECT_UNAUTHORIZED}
      - UNSUBSCRIBE_SECRET=${UNSUBSCRIBE_SECRET}
      - BASE_URL=${BASE_URL}
    volumes:
      - ${DATA_DIR}/web:/app/data
    networks:
      - privdna-internal

  rybbit-backend:
    image: ghcr.io/rybbit-io/rybbit-backend:latest
    container_name: rybbit-backend
    restart: unless-stopped
    environment:
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=5432
      - POSTGRES_DB=rybbit
      - POSTGRES_USER=rybbit
      - POSTGRES_PASSWORD=${RYBBIT_DB_PASSWORD}
      - CLICKHOUSE_HOST=http://clickhouse:8123
      - CLICKHOUSE_DB=analytics
      - CLICKHOUSE_USER=default
      - CLICKHOUSE_PASSWORD=${RYBBIT_CLICKHOUSE_PASSWORD}
      - BETTER_AUTH_SECRET=${RYBBIT_AUTH_SECRET}
      - BASE_URL=${RYBBIT_URL}
      - DISABLE_SIGNUP=true
    depends_on:
      - rybbit-postgres
      - rybbit-clickhouse
    networks:
      - privdna-internal

  rybbit-client:
    image: ghcr.io/rybbit-io/rybbit-client:latest
    container_name: rybbit-client
    restart: unless-stopped
    environment:
      - NEXT_PUBLIC_BACKEND_URL=${RYBBIT_URL}
      - NEXT_PUBLIC_DISABLE_SIGNUP=true
      - BASE_URL=${RYBBIT_URL}
    depends_on:
      - rybbit-backend
    networks:
      - privdna-internal

  rybbit-postgres:
    image: postgres:16-alpine
    container_name: rybbit-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_USER=rybbit
      - POSTGRES_PASSWORD=${RYBBIT_DB_PASSWORD}
      - POSTGRES_DB=rybbit
    volumes:
      - ${DATA_DIR}/rybbit/postgres:/var/lib/postgresql/data
    networks:
      privdna-internal:
        aliases:
          - postgres

  rybbit-clickhouse:
    image: clickhouse/clickhouse-server:latest
    container_name: rybbit-clickhouse
    restart: unless-stopped
    environment:
      - CLICKHOUSE_DB=analytics
      - CLICKHOUSE_USER=default
      - CLICKHOUSE_PASSWORD=${RYBBIT_CLICKHOUSE_PASSWORD}
    volumes:
      - ${DATA_DIR}/rybbit/clickhouse:/var/lib/clickhouse
    networks:
      privdna-internal:
        aliases:
          - clickhouse
    ulimits:
      nofile:
        soft: 262144
        hard: 262144

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    restart: unless-stopped
    command: tunnel run
    environment:
      - TUNNEL_TOKEN=${CLOUDFLARE_TUNNEL_TOKEN}
    networks:
      - privdna-internal
    depends_on:
      - privdna-web
      - rybbit-client

networks:
  privdna-internal:
    driver: bridge
